product:
  type: jvm/app

apply:
  - //shared/common.module-template.yaml

settings:
  jvm:
    mainClass: MainKt
  compose:
    version: 1.11.1
    enabled: true
    experimental:
      hotReload:
        version: 1.1.1

dependencies:
  - //shared
  - $libs.slf4j.simple
  - $compose.desktop.currentOs
# - $libs.compose2pdf
