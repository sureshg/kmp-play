product: jvm/amper-plugin

# repositories:
#  - https://packages.jetbrains.team/maven/p/amper/amper

description: Native Image plugin for the Kotlin Toolchain

dependencies:
  - $libs.ktor.client.cio: runtime-only

pluginInfo:
  id: native-image
  settingsClass: Settings
