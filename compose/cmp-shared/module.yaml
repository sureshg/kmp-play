product:
  type: kmp/lib
  platforms: [ jvm, wasmJs ]

apply:
  - //build-logic/templates/compose.module-template.yaml

settings:
  compose:
    resources:
      packageName: dev.suresh.gen
      exposedAccessors: true